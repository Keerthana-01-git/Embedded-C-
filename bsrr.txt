int main(void)
{

  /* USER CODE BEGIN 1 */

  /* USER CODE END 1 */

  /* MCU Configuration--------------------------------------------------------*/

  /* Reset of all peripherals, Initializes the Flash interface and the Systick. */
  HAL_Init();

  /* USER CODE BEGIN Init */

  /* USER CODE END Init */

  /* Configure the system clock */
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  /* Initialize all configured peripherals */
  /* USER CODE BEGIN 2 */

  RCC->AHBENR |= (1 << 17);              // Sets bit 17, for IOPAEN( I/O port A enable) and all other bits to 0.
  GPIOA->MODER  &= ~(3U << (5*2));		 // Clears 2 bits at position 10 and 11 to 00.
  GPIOA->MODER |= (1U << (5*2));	     // Sets those bits to 01, which is the output mode.

  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1)
  {
    /* USER CODE END WHILE */

	 GPIOA ->BSRR |= (1<<5);             // bit 5 of BSRR [15:0] to set
	 	 for (int i=0; i<500000; i++);    // software delay

	 GPIOA ->BSRR |= (1<<21);			// bit 21 of BSRR [16:32] to reset, where 21 is counter part of bit 5 set.
	 	 for (int i=0; i<500000; i++);    // software delay

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */


